# Local development environment configuration
# This file is used when running the frontend locally (outside Docker)

# Node environment
NODE_ENV=development

# Vite configuration
VITE_HMR=true
VITE_FAST_REFRESH=true

# API configuration - connects to the containerized backend
VITE_API_URL=http://localhost:8080

# No need for Docker-specific settings when running locally